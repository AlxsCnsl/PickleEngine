foreach(module_cmakelists ${MODULE_DIRS})
    get_filename_component(module_dir ${module_cmakelists} DIRECTORY)
    message(STATUS "🔗 Ajout du module: ${module_dir}")
    add_subdirectory(${module_dir})
endforeach()